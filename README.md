# svg-processor
A C-based SVG processing system that parses vector graphics and exports to bitmap formats (BMP/JPG). Features comprehensive SVG parsing for circles, rectangles, and lines with full geometric attribute extraction. Includes command-line tools and an interactive editor for real-time shape manipulation and conversion.
